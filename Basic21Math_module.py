# MATH MODULE
import math
x=4
y=2
#print(math.ceil(x))#It is used to find the smallest integer greater than or equal to a given number (ceiling).
#print(math.floor(x))#Opposite of ceiling
#print(math.sqrt(x))
print(math.pow(x,y))